import Store from 'herculex';
export default new Store({
    connectGlobal: true,
    state: {},
    getters: {},
    plugins: [],
    mutations: {},
    actions: {
        async pageOnLoad({ state, dispatch, commit, getters }) {
        },
    }
});
